
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile information.",
      "type": "object",
      "properties": {
        "loginId": {
          "type": "string",
          "description": "The user's unique login identifier."
        },
        "rating": {
          "type": "number",
          "description": "The user's ELO rating for ranked matches. Defaults to 1500."
        },
        "lastMatchDate": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp of the user's last completed match."
        }
      }
    },
    "Card": {
      "title": "Card",
      "description": "Represents a single card in the trading card game.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the card."
        },
        "theme": {
          "type": "string",
          "description": "The theme of the card (e.g., fantasy, sci-fi).",
          "enum": ["fantasy", "sci-fi", "modern", "custom"]
        },
        "name": {
          "type": "string",
          "description": "The name of the card."
        },
        "manaCost": {
          "type": "number",
          "description": "The mana cost to play the card."
        },
        "attack": {
          "type": "number",
          "description": "The attack power of the card (for creatures)."
        },
        "defense": {
          "type": "number",
          "description": "The defense/health of the card (for creatures)."
        },
        "cardType": {
          "type": "string",
          "description": "The type of the card.",
          "enum": ["creature", "spell", "artifact", "land"]
        },
        "rarity": {
          "type": "string",
          "description": "The rarity of the card.",
          "enum": ["common", "uncommon", "rare", "mythic"]
        },
        "abilities": {
          "type": "string",
          "description": "The special abilities or rules text of the card."
        },
        "flavorText": {
          "type": "string",
          "description": "The thematic or story text of the card."
        },
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL for the card's artwork."
        },
        "imageHint": {
          "type": "string",
          "description": "A hint for generating the card's image."
        },
        "frameImageUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL for the card's custom frame."
        }
      },
      "required": ["name", "manaCost", "cardType", "rarity", "imageUrl"]
    },
    "Game": {
      "title": "Game",
      "description": "Represents the state of an online card game session.",
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["waiting", "active", "finished"],
          "description": "The current status of the game."
        },
        "player1Id": {
          "type": "string",
          "description": "The UID of the first player."
        },
        "player2Id": {
          "type": "string",
          "description": "The UID of the second player."
        },
        "player1LoginId": {
          "type": "string",
          "description": "The login ID of player 1."
        },
        "player2LoginId": {
          "type": "string",
          "description": "The login ID of player 2."
        },
        "player1Health": {
          "type": "number",
          "description": "The current health of player 1."
        },
        "player2Health": {
          "type": "number",
          "description": "The current health of player 2."
        },
        "turn": {
          "type": "number",
          "description": "The current turn number."
        },
        "activePlayerId": {
          "type": "string",
          "description": "The UID of the player whose turn it is."
        },
        "winnerId": {
          "type": "string",
          "description": "The UID of the winning player, if the game is finished."
        },
        "lastAction": {
          "type": "string",
          "description": "A description of the last action taken."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the game was created."
        },
        "player1Deck": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
        "player2Deck": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
        "player1Board": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
        "player2Board": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
         "player1Hand": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
        "player2Hand": {
          "type": "array",
          "items": { "$ref": "#/entities/Card" }
        },
        "player1Mana": {
            "type": "number",
            "description": "Current mana for player 1."
        },
        "player2Mana": {
            "type": "number",
            "description": "Current mana for player 2."
        },
        "player1MaxMana": {
          "type": "number"
        },
        "player2MaxMana": {
            "type": "number"
        }
      }
    },
    "TradeOffer": {
      "title": "Trade Offer",
      "description": "Represents a trade offer between two users.",
      "type": "object",
      "properties": {
        "offerorId": { "type": "string", "description": "The UID of the user making the offer." },
        "offerorLoginId": { "type": "string", "description": "The login ID of the offeror." },
        "offereeId": { "type": "string", "description": "The UID of the user receiving the offer." },
        "offereeLoginId": { "type": "string", "description": "The login ID of the offeree." },
        "offeredCards": { "type": "array", "items": { "$ref": "#/entities/Card" } },
        "requestedCards": { "type": "array", "items": { "$ref": "#/entities/Card" } },
        "status": { "type": "string", "enum": ["pending", "accepted", "rejected", "cancelled"] },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["email", "google"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public information about a user, like their display name and rating.",
      "rules": [
        {
          "description": "Users can read their own profile and all profiles for ranking purposes.",
          "allow": ["read"]
        },
        {
          "description": "Users can only create or update their own profile.",
          "allow": ["create", "update"],
          "if": "request.auth != null && request.auth.uid == userId"
        }
      ]
    },
    "/games/{gameId}": {
      "schema": {
        "$ref": "#/entities/Game"
      },
      "description": "Stores individual game sessions for online battles.",
      "rules": [
        {
            "description": "Authenticated users can read, create and update games.",
            "allow": ["read", "create", "update"],
            "if": "request.auth != null"
        }
      ]
    },
    "/trades/{tradeId}": {
      "schema": {
        "$ref": "#/entities/TradeOffer"
      },
      "description": "Stores trade offers between users.",
      "rules": [
        {
            "description": "Authenticated users can create trade offers.",
            "allow": ["create"],
            "if": "request.auth != null"
        },
        {
            "description": "Users can read trade offers they are involved in.",
            "allow": ["read"],
            "if": "request.auth != null && (request.auth.uid == resource.data.offerorId || request.auth.uid == resource.data.offereeId)"
        },
        {
            "description": "The offeree can accept or reject a pending offer.",
            "allow": ["update"],
            "if": "request.auth != null && request.auth.uid == resource.data.offereeId && resource.data.status == 'pending' && (request.resource.data.status == 'accepted' || request.resource.data.status == 'rejected')"
        },
        {
            "description": "The offeror can cancel a pending offer.",
            "allow": ["update"],
            "if": "request.auth != null && request.auth.uid == resource.data.offerorId && resource.data.status == 'pending' && request.resource.data.status == 'cancelled'"
        }
      ]
    }
  }
}
